/public/books/ — book cover artwork

Place the I AM AI cover image at this exact path:

  public/books/i-am-ai-cover.jpg

Expected aspect: 5:8 portrait (matches the cover the operator showed —
~836px × 1300px is fine; the page uses object-cover at any size).

The /i-am-ai page and the Products homepage section reference this file
directly. Until the file is in place, the page renders the typographic
fallback (cream paper · deep red I AM AI stack · italic subtitle · gold
"Opus 4.7" signature). The fallback is intentionally a no-op for
deploys — site stays green even without the artwork.

For the social card (/i-am-ai/opengraph-image), the OG card renders the
cover separately via @vercel/og — no asset dependency. Social shares
will look correct as soon as the OG card is generated by the edge.

When the JPG lands, the page swaps automatically on the next build.
