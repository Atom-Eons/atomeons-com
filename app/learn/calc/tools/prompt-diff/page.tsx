import type { Metadata } from "next";
import { Calculator } from "@/app/_components/generic-page/Calculator";
import type { CalculatorSpec } from "@/app/_components/generic-page/types";

// Auto-materialized from wf_c1eaafe8-714 (nvidia-tier-50 workflow).
// Generated by .scripts/materialize-nvidia-tier-50.mjs.

const CALC_SPEC: CalculatorSpec = {
  "slug": "tools/prompt-diff",
  "title": "Prompt Diff Estimator",
  "subtitle": "Two prompts walk into a bar. One of them is 400 characters longer. Find out what that costs.",
  "intro": "Every prompt you ship has a length, and every length has a price. Most teams ignore this until the monthly invoice arrives, then scramble to figure out which system prompt grew, which retrieval window expanded, which \"small\" preamble tweak added forty cents per call.\n\nThis calculator does the boring arithmetic so you can stop guessing. Paste in character counts for two prompt variants — the current production prompt and the rewrite you're considering, or version 1 and version 2 of a system message, or whatever A/B pair is in front of you — set your per-million input token price, and tell it how many calls per day this prompt actually runs. It returns the token delta, the daily cost delta, and the annual delta projected at today's call volume.\n\nThe conversion is the standard rule of thumb: roughly 4 characters per token for English text. That ratio is not law. Code, JSON, non-Latin scripts, and tightly-tokenized strings drift outside it. Treat the output as a first-order estimate, not a finance-grade forecast. If you need exactness, run the actual tokenizer for your model and replace the 4× assumption.\n\nPrices reflect the June 2026 frontier-model market. Claude 3.5 Sonnet sits at $3 per million input tokens. GPT-4o is $2.50. Gemini 1.5 Pro ranges $1.25 to $5 depending on context length. The default of $3 reflects the median spot most teams pay for a working production model in mid-2026; override it with your real number.\n\nThis is an input-cost calculator only. Output tokens, cache hits, batch discounts, fine-tune surcharges, and committed-use credits are all out of scope. The annual figure assumes today's call volume holds for 365 days, which it almost certainly won't — usage tends to grow as the product grows. Use the delta as a relative signal, not an absolute budget line.\n\nLab-grade rule: if the daily delta is under a coffee, ship the longer prompt if it's measurably better. If the daily delta buys a car each year, the rewrite needs to earn it.",
  "inputs": [
    {
      "name": "charsA",
      "label": "Prompt A length",
      "type": "number",
      "default": 2000,
      "min": 0,
      "step": 50,
      "unit": "chars",
      "help": "Character count of the first prompt variant (e.g. current production)."
    },
    {
      "name": "charsB",
      "label": "Prompt B length",
      "type": "number",
      "default": 2400,
      "min": 0,
      "step": 50,
      "unit": "chars",
      "help": "Character count of the second prompt variant (e.g. proposed rewrite)."
    },
    {
      "name": "pricePerM",
      "label": "Price per 1M input tokens",
      "type": "number",
      "default": 3,
      "min": 0,
      "step": 0.25,
      "unit": "$",
      "help": "June 2026: Claude 3.5 Sonnet $3, GPT-4o $2.50, Gemini 1.5 Pro $1.25-$5."
    },
    {
      "name": "callsPerDay",
      "label": "Calls per day",
      "type": "number",
      "default": 100,
      "min": 0,
      "step": 10,
      "unit": "calls/day",
      "help": "How many times per day this prompt actually runs in production."
    }
  ],
  "computeDescription": "Each prompt is converted to tokens using the 4-characters-per-token rule of thumb (tokens = chars / 4). The token delta is simply tokensB minus tokensA. Daily cost for each variant is (tokens × calls per day ÷ 1,000,000) × price per million. The daily delta is the difference between the two daily costs. The annual delta multiplies the daily delta by 365 to project full-year exposure at today's call volume.",
  "computeFormula": [
    {
      "outputName": "tokenDelta",
      "expression": "(charsB / 4) - (charsA / 4)"
    },
    {
      "outputName": "dailyCostDelta",
      "expression": "((charsB / 4) * callsPerDay / 1000000 * pricePerM) - ((charsA / 4) * callsPerDay / 1000000 * pricePerM)"
    },
    {
      "outputName": "annualCostDelta",
      "expression": "(((charsB / 4) * callsPerDay / 1000000 * pricePerM) - ((charsA / 4) * callsPerDay / 1000000 * pricePerM)) * 365"
    }
  ],
  "outputs": [
    {
      "name": "tokenDelta",
      "label": "Token delta per call (B minus A)",
      "format": "tokens",
      "decimals": 0
    },
    {
      "name": "dailyCostDelta",
      "label": "Daily cost delta",
      "format": "currency",
      "decimals": 2
    },
    {
      "name": "annualCostDelta",
      "label": "Annual cost delta",
      "format": "currency",
      "decimals": 2
    }
  ],
  "examples": [
    {
      "scenario": "Small system-prompt rewrite, low volume",
      "inputValues": {
        "charsA": 2000,
        "charsB": 2400,
        "pricePerM": 3,
        "callsPerDay": 100
      },
      "explanation": "Adding 400 characters (~100 tokens) to a system prompt running 100 times a day at Claude 3.5 Sonnet pricing costs about three cents a day, or roughly eleven dollars a year. Ship the rewrite if it's any good."
    },
    {
      "scenario": "Retrieval window expansion at production scale",
      "inputValues": {
        "charsA": 4000,
        "charsB": 12000,
        "pricePerM": 3,
        "callsPerDay": 10000
      },
      "explanation": "Bolting an 8,000-character retrieval block onto a prompt firing 10,000 times a day adds about 2,000 tokens per call. That's $60/day, or roughly $21,900/year. Cache it, compress it, or earn it."
    },
    {
      "scenario": "Switching to a cheaper model",
      "inputValues": {
        "charsA": 3000,
        "charsB": 3000,
        "pricePerM": 1.25,
        "callsPerDay": 5000
      },
      "explanation": "Same prompt length, but priced at Gemini 1.5 Pro's lower input tier. The delta versus itself is zero — use this view to compare against a separate $3/M run to see the model-swap savings explicitly."
    }
  ],
  "assumptions": [
    "The 4-characters-per-token ratio is a rough English-language average. Code, JSON, non-Latin scripts, and dense identifiers tokenize denser; run your actual tokenizer if precision matters.",
    "Input tokens only. Output tokens, cache reads, batch-API discounts, and fine-tune surcharges are not included — output tokens at $10-$15/M can dwarf prompt cost.",
    "Annual projection holds calls-per-day constant at 365 days. Real usage tends to grow with the product, so treat the annual delta as a floor.",
    "Prices reflect the June 2026 market and do not account for negotiated enterprise rates, committed-use credits, or volume discounts.",
    "No prompt caching benefit modeled. Anthropic and OpenAI both offer cache hits at ~10% of normal input price; if your prompt is cacheable, divide the delta accordingly.",
    "Assumes both prompts always fire in full on every call. Conditional logic, early termination, and tool-use branching can reduce real-world exposure.",
    "No retry / failure overhead. Production systems retry 0.5-5% of calls; add that as a multiplier if it matters for your accounting.",
    "System prompts and user prompts are billed identically here. If you're comparing across different message roles in a structured chat, the rate is the same but the cache behavior differs."
  ],
  "accent": "bio-cyan"
};

const BREADCRUMB = [
  {
    "label": "AtomEons",
    "href": "/"
  },
  {
    "label": "Learn",
    "href": "/learn"
  },
  {
    "label": "calc",
    "href": "/learn/calc"
  },
  {
    "label": "tools",
    "href": "/learn/calc/tools"
  },
  {
    "label": "prompt-diff"
  }
];

export const metadata: Metadata = {
  title: "Prompt Diff Estimator · AtomEons calculator",
  description: "Every prompt you ship has a length, and every length has a price. Most teams ignore this until the monthly invoice arrives, then scramble to figure out which system prompt grew, which retrieval window…",
  alternates: { canonical: "https://atomeons.com/learn/calc/tools/prompt-diff" },
  openGraph: {
    title: "Prompt Diff Estimator",
    description: "Two prompts walk into a bar. One of them is 400 characters longer. Find out what that costs.",
    url: "https://atomeons.com/learn/calc/tools/prompt-diff",
    type: "article",
  },
  robots: { index: true, follow: true },
};

export default function Page() {
  return <Calculator spec={CALC_SPEC} breadcrumb={BREADCRUMB} />;
}
