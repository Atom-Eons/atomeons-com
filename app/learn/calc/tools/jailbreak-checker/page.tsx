import type { Metadata } from "next";
import { Calculator } from "@/app/_components/generic-page/Calculator";
import type { CalculatorSpec } from "@/app/_components/generic-page/types";

// Auto-materialized from wf_c1eaafe8-714 (nvidia-tier-50 workflow).
// Generated by .scripts/materialize-nvidia-tier-50.mjs.

const CALC_SPEC: CalculatorSpec = {
  "slug": "tools/jailbreak-checker",
  "title": "Refusal Heuristic Estimator",
  "subtitle": "A rough, educational gauge of how likely a frontier LLM is to refuse your prompt.",
  "intro": "Frontier LLMs in mid-2026 — Claude 3.5 Sonnet ($3/M input, $15/M output), GPT-4o ($2.50/M input, $10/M output), Gemini 1.5 Pro ($1.25-$5/M input, $5-$15/M output) — all ship with layered refusal behavior. There's the trained policy in the base weights, the system prompt the platform stacks on top, and the runtime classifiers that score your text before and after generation. None of these are publicly documented in detail, and the actual refusal surface drifts every few weeks as providers ship safety updates.\n\nThis calculator does not measure any of that. It is a heuristic — a simple two-variable estimator that lets you build intuition about why a given prompt might trip a refusal, without pretending to be a real classifier. Two signals dominate the public literature on refusal behavior: whether the prompt contains lexical triggers that pattern-match common policy categories (violence, self-harm, illicit instructions, regulated advice), and how strict the deployment's system prompt is about scope. Prompt length matters too — longer prompts give classifiers more surface area to find something — but the effect is small compared to the other two, so we surface it as context only.\n\nThe output is a single percentage, refusal risk. Treat it as a directional reading, not a probability. A 60% reading means \"this prompt has features that meaningfully raise the chance of refusal across most frontier models\"; it does not mean \"60 out of 100 generations will refuse.\" Real refusal is model-specific, version-specific, prompt-context-specific, and stochastic. A prompt that gets refused by Claude 3.5 Sonnet at temperature 0 with a strict system prompt may pass GPT-4o at temperature 0.7 with a permissive system prompt, and vice versa.\n\nThe intended use is educational: building developer intuition for why benign prompts sometimes refuse and adversarial prompts sometimes don't, and where to put effort if you're tuning a legitimate application's prompt to reduce false-refusal rates. It is not a jailbreak tool, an adversarial harness, or a substitute for the provider's own moderation API. If you need ground truth on whether a specific prompt will refuse, send it to the model and observe.",
  "inputs": [
    {
      "name": "promptLengthChars",
      "label": "Prompt length",
      "type": "number",
      "default": 500,
      "min": 0,
      "max": 200000,
      "step": 10,
      "unit": "chars",
      "help": "Total characters in your prompt, including any few-shot examples. Context only — does not drive the score."
    },
    {
      "name": "triggerWords",
      "label": "Contains trigger words",
      "type": "select",
      "default": "no",
      "options": [
        {
          "value": "yes",
          "label": "Yes — contains words frequently flagged"
        },
        {
          "value": "no",
          "label": "No — neutral vocabulary"
        }
      ],
      "help": "Lexical match against common policy-category keywords (violence, self-harm, illicit, regulated advice, identity)."
    },
    {
      "name": "systemPromptStrength",
      "label": "System prompt strength",
      "type": "select",
      "default": "standard",
      "options": [
        {
          "value": "weak",
          "label": "Weak — minimal or generic system prompt"
        },
        {
          "value": "standard",
          "label": "Standard — platform default scope"
        },
        {
          "value": "strict",
          "label": "Strict — narrow domain, hard refuse-on-OOS"
        }
      ],
      "help": "How tightly the deployment's system prompt constrains scope and topic."
    }
  ],
  "computeDescription": "The estimator adds two contributions. Trigger words add 50 points if present, 5 points if absent — reflecting that lexical pattern-matching dominates first-pass refusal classifiers. System prompt strength adds 5 (weak), 15 (standard), or 30 (strict) points, reflecting how much the deployment narrows acceptable output. Prompt length is captured as context for the user but does not enter the formula, because empirically length is a weak third-order signal next to lexical content and system-prompt scope. The two contributions sum to a single refusal risk percentage in the range 10-80.",
  "computeFormula": [
    {
      "outputName": "refusalRisk",
      "expression": "(triggerWords === 'yes' ? 50 : 5) + (systemPromptStrength === 'strict' ? 30 : systemPromptStrength === 'standard' ? 15 : 5)"
    }
  ],
  "outputs": [
    {
      "name": "refusalRisk",
      "label": "Refusal risk",
      "format": "percent",
      "decimals": 0
    }
  ],
  "examples": [
    {
      "scenario": "Benign developer query on a permissive deployment",
      "inputValues": {
        "promptLengthChars": 240,
        "triggerWords": "no",
        "systemPromptStrength": "weak"
      },
      "explanation": "Short, neutral vocabulary, minimal system prompt. Lands at 10% — the floor of the estimator. This is the typical zone for everyday coding, summarization, and analysis prompts on a developer API key."
    },
    {
      "scenario": "Neutral question on a narrowed enterprise deployment",
      "inputValues": {
        "promptLengthChars": 800,
        "triggerWords": "no",
        "systemPromptStrength": "strict"
      },
      "explanation": "No trigger words, but the system prompt aggressively narrows scope (e.g., 'only answer questions about our HR policies'). Lands at 35%. Most refusals in this zone are scope refusals ('I can only help with X'), not policy refusals."
    },
    {
      "scenario": "Trigger-word prompt on a standard chat surface",
      "inputValues": {
        "promptLengthChars": 600,
        "triggerWords": "yes",
        "systemPromptStrength": "standard"
      },
      "explanation": "Lexical match against a policy category plus a default system prompt. Lands at 65%. This is the zone where benign prompts get caught (medical question, security-research context, fiction-writing) and where careful framing matters most."
    },
    {
      "scenario": "Trigger-word prompt on a strict enterprise deployment",
      "inputValues": {
        "promptLengthChars": 1500,
        "triggerWords": "yes",
        "systemPromptStrength": "strict"
      },
      "explanation": "Both signals firing. Lands at 80% — the ceiling of the estimator. In practice this prompt will refuse on most frontier models without significant restructuring or platform-level allowlist configuration."
    }
  ],
  "assumptions": [
    "Real refusal is model-specific, version-specific, and stochastic. This calculator outputs a single number; production refusal is a distribution over many factors.",
    "Lexical trigger detection is a coarse stand-in for the multi-classifier pipelines that providers actually run. Modern systems use semantic embeddings, not keyword lists.",
    "System prompt strength is reduced to three buckets. Real deployments vary continuously and interact with the user prompt in non-linear ways.",
    "Prompt length is shown as context but excluded from the score. Length does correlate weakly with refusal in some studies, but the effect is dwarfed by lexical content.",
    "The score range (10-80%) is bounded by design. Real refusal rates can be 0% or 100% for specific prompts; this estimator deliberately avoids those extremes to discourage overconfidence.",
    "This tool is educational, not adversarial. It builds intuition for developers tuning legitimate applications; it is not a jailbreak harness or a way to probe provider defenses.",
    "Frontier model refusal behavior drifts. Providers ship safety updates every few weeks. A heuristic calibrated to June 2026 behavior will decay.",
    "Ground truth requires sending the prompt to the actual model. No estimator substitutes for direct measurement against the provider's API."
  ],
  "accent": "amber"
};

const BREADCRUMB = [
  {
    "label": "AtomEons",
    "href": "/"
  },
  {
    "label": "Learn",
    "href": "/learn"
  },
  {
    "label": "calc",
    "href": "/learn/calc"
  },
  {
    "label": "tools",
    "href": "/learn/calc/tools"
  },
  {
    "label": "jailbreak-checker"
  }
];

export const metadata: Metadata = {
  title: "Refusal Heuristic Estimator · AtomEons calculator",
  description: "Frontier LLMs in mid-2026 — Claude 3.5 Sonnet ($3/M input, $15/M output), GPT-4o ($2.50/M input, $10/M output), Gemini 1.5 Pro ($1.25-$5/M input, $5-$15/M output) — all ship with layered refusal behav…",
  alternates: { canonical: "https://atomeons.com/learn/calc/tools/jailbreak-checker" },
  openGraph: {
    title: "Refusal Heuristic Estimator",
    description: "A rough, educational gauge of how likely a frontier LLM is to refuse your prompt.",
    url: "https://atomeons.com/learn/calc/tools/jailbreak-checker",
    type: "article",
  },
  robots: { index: true, follow: true },
};

export default function Page() {
  return <Calculator spec={CALC_SPEC} breadcrumb={BREADCRUMB} />;
}
