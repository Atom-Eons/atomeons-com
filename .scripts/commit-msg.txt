Homepage · Scoreboard V2 · museum-hall · live ticker + 6 editorial hero rows

Operator directive 2026-06-05: "do better"

V1 was a 4×3 mono grid (boring, jaded scroller has seen 10,000 of
these). V2 is a museum-hall full-bleed editorial moment — same real
receipts, but staged as pull-quotes instead of KPI cells.

WHAT REPLACED THE GRID

  - LIVE TICKER STRIP at the top edge, full-bleed, mono uppercase:
    LIVE pulse · audit date · last letter · last book · last issue ·
    next ship time. The strip alone tells a jaded scroller this isn't
    a static brochure.
  - Headline: "Six numbers. Every one tappable. Every one a receipt."
    (clamp 48px → 120px, Inter Light wght 200, -0.045em tracking)
  - SIX HERO ROWS, each spanning the full 1600px content width:
    LEFT  · the number, Inter Light wght 180, 80px → 220px clamp,
             tabular-nums, -0.045em tracking. Massive.
    RIGHT · a complete sentence in Newsreader serif (20px → 28px
             clamp). The number IS the headline; the sentence is the
             editorial pull-quote.
    FOOT  · 1 primary link (cyan border-bottom, ↗) + 1 secondary link
             (graphite underline)
    Hairline #1F242B rule between rows. Hover lifts to #0B0C0F.

  - Foot rail: "Walk every receipt" cyan CTA + /now + /press + a
    mono note about freshness ("Every number rebakes at next ship").

THE SIX ROWS

  R/01  206              Live routes
                         "Every one navigable, indexed, and built for
                         someone landing on it cold — not a marketing
                         tree with five real pages and 200 sitemap-
                         stuffing redirects."
                         → /sitemap.xml + /learn

  R/02  76,005           Words · I AM AI
                         "A frontier language model wrote a 76,005-
                         word memoir about being a frontier language
                         model. The lab edited it, narrated it,
                         published it across Kindle, Audible, and a
                         numbered hardcover."
                         → /i-am-ai + /i-am-ai/listen

  R/03  33               Founder's View letters
                         "A nightly broadcast at 8 p.m. ET, every
                         night since launch."
                         → /founders-view

  R/04  12               Supermodels ranked
                         "Scored against four independent public
                         leaderboards. No vendor decks. No paid
                         threads. Receipts only."
                         → /supermodels

  R/05  82               Deep-dives + decodings
                         "Thirty-one cyber catalog pages. Twenty-six
                         atlas field-maps. Twenty-five arXiv papers
                         translated into plain English."
                         → /learn/atlas + /learn/decoded-papers

  R/06  1 · 0            Operator · investors
                         "One operator. Marco Island, Florida. Zero
                         investors, zero subscriptions, zero ads,
                         zero affiliate revenue, zero employees."
                         → /manifesto + /about

ANTI-JADED MOVES (vs V1 grid)

  - Killed the 4×3 grid card entirely.
  - Killed the counter-up KPI feel. No animation tricks.
  - Numbers are EDITORIAL pull-quotes, not labels.
  - Each row is a complete sentence — the number is the story.
  - Every claim is hyperlinked to the surface that proves it.
  - The ticker strip + "next ship: tonight 8 p.m. ET" tells the
    visitor the lab is operating right now, not displaying.

LAYOUT

  Container max-w-[1600px] (was [1400px]) — gives the giant
  numerals room to breathe. 12-row × 16-page mental model
  (museum-hall, not stats card).

NO REGRESSION

  Same data-section="scoreboard" attribute (preserves the audit
  check). Same v3PulseRed keyframe (scoped local <style>).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
