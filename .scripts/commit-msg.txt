Scoreboard V3 · human-readable + marketing-effect copy pass

Operator directive 2026-06-05: "for humans needs to be readable and
marketing effect"

V2 (museum-hall) had the right SHAPE but the copy was too editorial —
50-word pull-quotes a tired scroller could not scan. V3 keeps the
museum-hall layout (big numbers + Newsreader serif + hover lift) and
rewrites every line for a 2-second read with a clear next action.

ROW REORDER · sales-funnel

  The order changed to lead with the strongest pull. I AM AI (the
  book) is now R/01 — it is the single most marketable artifact the
  lab has shipped this season. Routes count moved from R/01 to R/05
  (still on the list, no longer the lead).

  R/01  76,005   the book an AI wrote about itself
  R/02  33       letters · one every night
  R/03  12       AIs ranked by real reasoning
  R/04  82       deep dives · free, CC-BY
  R/05  206      live pages · all real
  R/06  1 · 0    operator · investors

COPY · two-clause pattern

  Each sentence now follows: <STRONG ASSERTION>.<one-line proof>.

  R/01  "An AI wrote a memoir about itself. 76,005 words.
         24 chapters. Read Chapter 1 free. Listen to Chapter 20 free."

  R/02  "A new letter every night at 8 p.m. ET. Real intelligence,
         decoded. Defense, AI, cyber, supply chain. Written like a
         manuscript. Free."

  R/03  "Which AI is actually best at thinking? May 2026 rankings
         against four independent benchmarks. No vendor decks,
         no paid threads. Receipts only."

  R/04  "82 long-form teach-yourself pages across cyber, AI atlas,
         and decoded research. Free. Open license. The curriculum a
         working operator would actually read."

  R/05  "206 real pages, not a marketing tree. Every one navigable,
         indexed, and built for someone landing on it cold.
         Hit ⌘K and search anything."

  R/06  "Built by one person. No outside capital. Marco Island,
         Florida. Zero investors. Zero subscriptions. Zero ads. The
         lab uses the tools it sells — and sells what it uses."

  Strong asserts render as #F4F4F2 (cream). Proof line drops to
  #9CA3AF (graphite). Scanner's eye lands on the assert first.

INTERACTION · the numbers are now hyperlinks

  V2 made the numbers passive. V3 wraps each number cell in
  <Link href={row.primary.href}> with proper aria-label. Hovering
  the row lifts both:
    - color: #F4F4F2 → #22F0D5 (cream → cyan)
    - font-variation-settings: wght 180 → 320 (Variable axis ramp)
  Click anywhere on the giant 220px number, drop on the surface.

HEADLINE

  V2: "Six numbers. Every one tappable. Every one a receipt."
  V3: "Six things the lab built. Tap any number." (cyan accent on
      the verb)

  Subhead V2 (54 words): "No projections. No inflated KPIs. No
  'happy customers.' Each row below is a complete sentence — the
  number is the headline, the link is the proof."

  Subhead V3 (29 words): "Real pages. Real benchmarks. Real audio.
  No projections, no inflated KPIs, no 'happy customers.' Each row
  drops you straight onto the thing."

CTA · directionals

  Each row's primary link uses → instead of ↗ to read as "drop you
  on the page" (in-site) rather than "open external."

NO REGRESSION

  - Live ticker strip unchanged
  - Foot rail (/receipts CTA + /now + /press + freshness note)
    unchanged
  - PulseRed keyframe unchanged
  - data-section="scoreboard" preserved
  - max-w-[1600px] preserved
  - All six surfaces verifiable on live site

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
